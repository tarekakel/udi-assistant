# Copy this file to .env and fill in your values. .env is git-ignored and must never be committed.

# OpenAI API key for the regulatory assistant (RAG) features. Create one at https://platform.openai.com/api-keys
OPENAI_API_KEY=
# Optional: override the chat model (default gpt-4o-mini)
OPENAI_MODEL=
